<UserControl x:Class="MaloobaFingerprint.Indicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeightHeight="30"
             d:DesignWidthWidth="30">

    <UserControl.Template>
        <ControlTemplate>
            <Grid x:Name="me">
                <Rectangle x:Name="lamp" Fill="{StaticResource DarkerBrush}" Stroke="Black"/>
            </Grid>
            <ControlTemplate.Triggers>
                <DataTrigger Binding="{Binding IsLit, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter TargetName="lamp" Property="Fill" Value="{StaticResource PurestGreenBrush}"/>
                </DataTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
