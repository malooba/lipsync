<UserControl x:Class="MaloobaLipSync.IndicatorLabel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="30">
    <UserControl.Template>
        <ControlTemplate>
            <Label Name="lamp" Content="{Binding Content , RelativeSource={RelativeSource AncestorType=UserControl}}" Foreground="Black" Background="Transparent"/>
            <ControlTemplate.Triggers>
                <DataTrigger Binding="{Binding IsLit, RelativeSource={RelativeSource Self}}" Value="True">
                    <Setter TargetName="lamp" Property="Foreground" Value="{StaticResource PurestGreenBrush}" />
                </DataTrigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </UserControl.Template>
</UserControl>
